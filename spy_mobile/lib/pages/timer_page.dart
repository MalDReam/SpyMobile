import 'package:flutter/material.dart';

class TimerPage extends StatelessWidget {
  const TimerPage({super.key, required int time});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}